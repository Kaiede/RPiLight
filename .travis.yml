os:
  - linux
  - osx
language: generic
sudo: required
dist: trusty
osx_image: xcode9.4
install:
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; eval "$(curl -sL https://swiftenv.fuller.li/install.sh)"
script:
  - swift package reset
  - swift build



