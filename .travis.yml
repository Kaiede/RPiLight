os:
  - linux
  - osx
language: generic
sudo: required
dist: trusty
osx_image: xcode9.4
install:
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then eval "$(curl -sL https://swiftenv.fuller.li/install.sh)"; fi
script:
  - swift package reset
  - swift build



