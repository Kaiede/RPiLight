os:
  - linux
  - osx
language: generic
sudo: required
dist: trusty
osx_image: xcode9.4
install:
  - eval "$(curl -sL https://swiftenv.fuller.li/install.sh)"
script:
  - . ~/.swiftenv/init
  - swift package reset
  - swift build



